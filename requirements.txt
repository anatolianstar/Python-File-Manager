# Python File Manager - Requirements
# This file contains only external packages that need to be installed via pip
# All other modules come from Python standard libraries

# Core dependencies (all included with Python)
# tkinter - GUI framework (included with Python)
# hashlib - File hashing (included with Python)
# threading - Multi-threading support (included with Python)
# shutil - High-level file operations (included with Python)
# json - JSON data handling (included with Python)
# os - Operating system interface (included with Python)
# sys - System-specific parameters (included with Python)
# stat - File status (included with Python)
# time - Time-related functions (included with Python)
# collections - Specialized container datatypes (included with Python)
# pathlib - Object-oriented filesystem paths (included with Python)
# traceback - Print or retrieve a stack traceback (included with Python)

# Optional dependencies (uncomment if needed):
# aiofiles>=23.1.0  # For async file operations
# pywin32>=305      # For Windows file system information

# For creating executable:
pyinstaller>=5.0

# No additional packages required for basic functionality! 